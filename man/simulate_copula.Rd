% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula.R
\name{simulate_copula}
\alias{simulate_copula}
\title{Simulate correlated general-distribution variables with a Gaussian copula}
\usage{
simulate_copula(
  n,
  specification,
  rho = 0,
  tau = NULL,
  structure = c("independent", "exchangeable", "ar1", "custom"),
  correlation = NULL,
  id = "id",
  seed = NULL,
  envir = parent.frame()
)
}
\arguments{
\item{n}{Number of observations.}

\item{specification}{Variable definitions with supported marginal
distributions.}

\item{rho}{Optional latent Pearson correlation.}

\item{tau}{Optional Kendall correlation, overriding \code{rho}.}

\item{structure}{Correlation structure: one of \code{"independent"},
\code{"exchangeable"}, \code{"ar1"}, or \code{"custom"}. If left unset it is chosen from
the other arguments: \code{"custom"} when \code{correlation} is supplied,
\code{"exchangeable"} when a non-zero \code{rho} (or \code{tau}) is supplied, and
\code{"independent"} otherwise. Passing \code{"independent"} together with a
non-zero \code{rho} is a contradiction and raises an error.}

\item{correlation}{Custom latent correlation matrix or tidy table.}

\item{id}{Identifier name.}

\item{seed}{Optional random seed.}

\item{envir}{Formula evaluation environment.}
}
\value{
A \code{simulab_sim} base \code{data.frame} with one row per observation and a
tidy latent-correlation table.
}
\description{
Simulate correlated general-distribution variables with a Gaussian copula
}
\examples{
specification <- define_variables(
  define_variable("normal_var", formula = "0", variance = "1", distribution = "normal"),
  define_variable("count_var", formula = "3", variance = "0", distribution = "poisson")
)
result <- simulate_copula(n = 200, specification = specification, rho = 0.5, seed = 1)
head(result)
components(result)
}
