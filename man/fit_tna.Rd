% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tna.R
\name{fit_tna}
\alias{fit_tna}
\title{Fit a temporal network analysis model}
\usage{
fit_tna(
  data,
  model = c("tna", "ftna", "ctna", "atna"),
  format = c("auto", "long", "wide"),
  id = "id",
  period = "period",
  state = "state",
  group = NULL,
  ...
)
}
\arguments{
\item{data}{Long- or wide-form sequence data.}

\item{model}{TNA, frequency TNA, co-occurrence TNA, or attention TNA.}

\item{format}{Input format. \code{auto} recognizes canonical long data.}

\item{id, period, state}{Canonical long-form column names.}

\item{group}{Optional grouping-column name. Supplying it fits a model per
group through the corresponding grouped TNA estimator.}

\item{...}{Additional arguments passed to the estimator in \code{tna}.}
}
\value{
A tidy edge-list \code{simulab_sim}. Initial probabilities and model
metadata are components; use \code{as_tna_model()} when a native model object is
required by \code{tna} plotting or inference functions.
}
\description{
Fit a temporal network analysis model
}
\examples{
data <- simulate_sequences(n = 40, n_states = 3, chain_length = 12, seed = 1)
if (requireNamespace("tna", quietly = TRUE)) {
  head(fit_tna(data, model = "tna"))
}
}
