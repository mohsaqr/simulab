% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequences.R
\name{trim_events}
\alias{trim_events}
\title{Trim longitudinal records at an event occurrence}
\usage{
trim_events(data, id, period, event, occurrence = 1L, event_value = 1)
}
\arguments{
\item{data}{Long-form base \code{data.frame}.}

\item{id}{Unit identifier.}

\item{period}{Ordered period variable.}

\item{event}{Event indicator variable.}

\item{occurrence}{Event occurrence at which to stop.}

\item{event_value}{Value identifying an event.}
}
\value{
A \code{simulab_sim} base \code{data.frame} retaining observations through the
requested event, or all observations when it never occurs.
}
\description{
Trim longitudinal records at an event occurrence
}
\examples{
# A long-form event log: `simulate_until_event()` generates the indicator,
# `trim_events()` then keeps records through its first occurrence.
data <- simulate_until_event(
  expand_periods(data.frame(id = 1:20), periods = 10,
                 id = "id", period = "period"),
  definition = define_variable("event", formula = "0.25",
                               distribution = "binary"),
  seed = 1
)

result <- trim_events(
  data, id = "id", period = "period", event = "event", occurrence = 1
)
head(result)
}
