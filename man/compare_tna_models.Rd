% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tna.R
\name{compare_tna_models}
\alias{compare_tna_models}
\title{Compare multiple TNA estimators on the same sequences}
\usage{
compare_tna_models(
  data,
  models = c("tna", "ftna", "ctna", "atna"),
  format = c("auto", "long", "wide"),
  id = "id",
  period = "period",
  state = "state",
  group = NULL,
  ...
)
}
\arguments{
\item{data}{Sequence data accepted by \code{fit_tna()}.}

\item{models}{One or more TNA model types.}

\item{format, id, period, state, group}{Input arguments passed to \code{fit_tna()}.}

\item{...}{Estimator arguments.}
}
\value{
A tidy edge-list \code{simulab_sim} base \code{data.frame} with one row per
model/group/edge and columns \code{model}, \code{from}, \code{to}, and \code{weight}
(with \code{group} inserted when \code{group} is supplied). A stacked \code{model_info}
table, one row per fitted model and group, is available through
\code{as.data.frame()}. The fitted native models are stored as a named list and
returned by \code{as_tna_model()}.
}
\description{
Compare multiple TNA estimators on the same sequences
}
\examples{
data <- simulate_sequences(n = 40, n_states = 3, chain_length = 12, seed = 1)
if (requireNamespace("tna", quietly = TRUE)) {
  compare_tna_models(data, models = c("tna", "ftna"))
}
}
