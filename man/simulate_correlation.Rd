% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{simulate_correlation}
\alias{simulate_correlation}
\title{Simulate a correlation design}
\usage{
simulate_correlation(
  n,
  means = 0,
  sds = 1,
  rho = 0,
  structure = c("independent", "exchangeable", "ar1", "custom"),
  correlation = NULL,
  variable_names = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations.}

\item{means, sds, rho, structure, correlation, variable_names, seed}{Arguments passed
to \code{simulate_correlated()}.}
}
\value{
A \code{simulab_sim} base \code{data.frame}.
}
\description{
Simulate a correlation design
}
\examples{
result <- simulate_correlation(n = 200, means = c(0, 0), sds = c(1, 1), rho = 0.6,
                               seed = 1)
head(result)
as.data.frame(result, what = "correlation")
}
