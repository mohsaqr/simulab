% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learning_states.R
\name{learning_states}
\alias{learning_states}
\title{List categorized learning states for sequence simulation}
\usage{
learning_states(categories = "all")
}
\arguments{
\item{categories}{A character vector of one or more category identifiers
from \code{\link[=learning_state_categories]{learning_state_categories()}}, or \code{"all"} (the default) for every
category. Unknown identifiers raise an error.}
}
\value{
A tidy base \code{data.frame} with one row per category-state pair and
columns \code{category} and \code{state}. A state may belong to more than one
category and then appears once per category, so \code{state} is not unique:
the full catalogue has 209 rows covering 202 distinct states.
}
\description{
List categorized learning states for sequence simulation
}
\examples{
head(learning_states())
head(learning_states(categories = "metacognitive"))
}
