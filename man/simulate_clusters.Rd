% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical.R
\name{simulate_clusters}
\alias{simulate_clusters}
\title{Simulate Gaussian clusters}
\usage{
simulate_clusters(
  n,
  centers,
  sds = 1,
  proportions = NULL,
  labels = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{centers}{Matrix with one row per cluster and one column per variable,
or a tidy data frame with columns \code{cluster}, \code{variable} and \code{center}.}

\item{sds}{Scalar, per-variable vector, cluster-by-variable matrix, or a
tidy data frame with columns \code{cluster}, \code{variable} and \code{sd}.}

\item{proportions}{Optional cluster proportions.}

\item{labels}{Optional cluster labels.}

\item{seed}{Optional random seed.}
}
\value{
A \code{simulab_sim} base \code{data.frame} with true cluster membership and
tidy center/scale tables.
}
\description{
Simulate Gaussian clusters
}
\examples{
# `centers` is one row per cluster and one column per variable.
result <- simulate_clusters(
  n = 150,
  centers = matrix(c(0, 0, 4, 4, 0, 4), nrow = 3, byrow = TRUE),
  sds = 1,
  seed = 1
)
head(result)
as.data.frame(result, what = "parameters")
}
