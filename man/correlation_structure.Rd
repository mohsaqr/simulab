% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{correlation_structure}
\alias{correlation_structure}
\title{Create a tidy correlation structure}
\usage{
correlation_structure(
  n_variables,
  rho = 0,
  structure = c("independent", "exchangeable", "ar1", "custom"),
  correlation = NULL,
  variable_names = NULL
)
}
\arguments{
\item{n_variables}{Number of variables.}

\item{rho}{Correlation coefficient used by exchangeable and AR(1)
structures.}

\item{structure}{Correlation structure: one of \code{"independent"},
\code{"exchangeable"}, \code{"ar1"}, or \code{"custom"}. If left unset it is chosen from
the other arguments: \code{"custom"} when \code{correlation} is supplied,
\code{"exchangeable"} when a non-zero \code{rho} (or \code{tau}) is supplied, and
\code{"independent"} otherwise. Passing \code{"independent"} together with a
non-zero \code{rho} is a contradiction and raises an error.}

\item{correlation}{Custom correlation matrix.}

\item{variable_names}{Optional variable names.}
}
\value{
A base \code{data.frame} with one row per matrix cell and columns \code{row},
\code{column}, and \code{correlation}.
}
\description{
Create a tidy correlation structure
}
\examples{
correlation_structure(
  n_variables = 3,
  rho = 0.5,
  structure = "ar1"
)
}
