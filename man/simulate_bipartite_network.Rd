% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_formats.R
\name{simulate_bipartite_network}
\alias{simulate_bipartite_network}
\title{Simulate a bipartite network}
\usage{
simulate_bipartite_network(
  actors,
  events,
  edges = NULL,
  probability = 0.1,
  weight = c("binary", "uniform", "normal", "poisson"),
  weight_mean = 1,
  weight_sd = 1,
  weight_range = c(0.1, 1),
  seed = NULL
)
}
\arguments{
\item{actors}{Number or labels of primary-mode nodes.}

\item{events}{Number or labels of secondary-mode nodes.}

\item{edges}{Optional exact number of cross-mode edges.}

\item{probability}{Cross-mode edge probability when \code{edges} is \code{NULL}.}

\item{weight}{Edge-weight distribution.}

\item{weight_mean, weight_sd, weight_range}{Weight parameters.}

\item{seed}{Optional seed.}
}
\value{
A tidy cross-mode edge-list \code{simulab_sim} base \code{data.frame} with one
row per actor-event edge and columns \code{from} (always an actor), \code{to}
(always an event), \code{weight}, \code{from_mode}, and \code{to_mode}. The two modes are
disjoint, so no edge joins two actors or two events and self-loops cannot
occur. \code{as.data.frame(x, what = )} also returns \code{nodes} (\code{node}, \code{mode}),
\code{incidence} (the wide actor-by-event incidence table: an \code{actor} column
followed by one column per event), and a one-row \code{settings} table.
}
\description{
Simulate a bipartite network
}
\examples{
result <- simulate_bipartite_network(actors = 20, events = 6, probability = 0.3, seed = 1)
head(result)
components(result)
}
