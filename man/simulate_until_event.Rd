% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequences.R
\name{simulate_until_event}
\alias{simulate_until_event}
\title{Simulate events and retain records through the nth event}
\usage{
simulate_until_event(
  data,
  definition,
  occurrence = 1L,
  id = "id",
  period = "period",
  seed = NULL,
  envir = parent.frame()
)
}
\arguments{
\item{data}{Long-form base \code{data.frame}.}

\item{definition}{One binary variable definition from \code{define_variable()}.}

\item{occurrence, id, period}{Event trimming arguments.}

\item{seed}{Optional random seed.}

\item{envir}{Formula evaluation environment.}
}
\value{
A \code{simulab_sim} base \code{data.frame} with the generated event indicator
and records through the requested occurrence.
}
\description{
Simulate events and retain records through the nth event
}
\examples{
# Long-form input: one row per unit per period.
data <- expand_periods(data.frame(id = 1:30), periods = 8,
                       id = "id", period = "period")

result <- simulate_until_event(
  data,
  definition = define_variable("event", formula = "0.3", distribution = "binary"),
  occurrence = 1,
  seed = 1
)
head(result)
}
