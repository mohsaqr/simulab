% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequences.R
\name{augment_markov}
\alias{augment_markov}
\title{Add a Markov chain to each input row}
\usage{
augment_markov(
  data,
  transition,
  chain_length,
  initial = NULL,
  states = NULL,
  trim_state = NULL,
  id = "id",
  period = "period",
  state = "state",
  seed = NULL
)
}
\arguments{
\item{data}{Base \code{data.frame} with a unique identifier.}

\item{transition, chain_length, states, trim_state}{Markov-chain arguments.}

\item{initial}{Starting-state probabilities or the name of an input variable
containing each row's starting state.}

\item{id, period, state}{Output variable names.}

\item{seed}{Optional random seed.}
}
\value{
A long-form \code{simulab_sim} base \code{data.frame} combining input variables
with one row per chain position.
}
\description{
Add a Markov chain to each input row
}
\examples{
data <- data.frame(id = 1:20, group = rep(c("a", "b"), each = 10))

result <- augment_markov(
  data,
  transition = matrix(c(0.7, 0.3, 0.4, 0.6), nrow = 2, byrow = TRUE),
  chain_length = 10,
  states = c("A", "B"),
  seed = 1
)
head(result)
}
