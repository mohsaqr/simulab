% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networks.R
\name{simulate_network}
\alias{simulate_network}
\title{Simulate a network from common graph models}
\usage{
simulate_network(
  nodes,
  model = c("bernoulli", "barabasi_albert", "small_world", "block", "regular",
    "geometric", "forest_fire"),
  probability = 0.1,
  edges = NULL,
  directed = TRUE,
  loops = FALSE,
  weight = c("binary", "uniform", "normal", "poisson"),
  weight_mean = 1,
  weight_sd = 1,
  weight_range = c(0.1, 1),
  node_type = NULL,
  edge_classes = NULL,
  class_probabilities = NULL,
  attachment = 2L,
  power = 1,
  neighbors = 2L,
  rewire = 0.05,
  blocks = 3L,
  within_probability = 0.3,
  between_probability = 0.05,
  degree = 4L,
  radius = 0.25,
  forward_probability = 0.35,
  backward_probability = 0.32,
  seed = NULL
)
}
\arguments{
\item{nodes}{Number of nodes or node labels.}

\item{model}{Graph model.}

\item{probability}{Bernoulli edge probability, node matrix, or type matrix.
A matrix may instead be given as a tidy data frame with columns \code{from},
\code{to} and \code{probability}.}

\item{edges}{Exact edge count for the fixed-edge Bernoulli model.}

\item{directed}{Generate directed edges.}

\item{loops}{Permit self-loops.}

\item{weight}{Edge-weight distribution.}

\item{weight_mean, weight_sd, weight_range}{Weight parameters.}

\item{node_type}{Optional type label for each node.}

\item{edge_classes}{Optional number or labels of edge classes.}

\item{class_probabilities}{Optional edge-class probabilities.}

\item{attachment, power}{Preferential-attachment parameters.}

\item{neighbors, rewire}{Small-world parameters.}

\item{blocks, within_probability, between_probability}{Block-model parameters.}

\item{degree}{Regular-graph degree.}

\item{radius}{Geometric-graph connection radius.}

\item{forward_probability, backward_probability}{Forest-fire parameters.}

\item{seed}{Optional random seed.}
}
\value{
A tidy edge-list \code{simulab_sim}; nodes, adjacency, and generation
settings are components. Use \code{as_igraph()} for native graph workflows.
}
\description{
Simulate a network from common graph models
}
\examples{
result <- simulate_network(
  nodes = 60, model = "bernoulli", probability = 0.06, seed = 1
)
head(result)
components(result)

# Other generators: barabasi_albert, small_world, block, regular,
# geometric and forest_fire.
head(simulate_network(nodes = 60, model = "small_world", neighbors = 2, seed = 1))
}
