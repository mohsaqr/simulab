% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_formats.R
\name{simulate_temporal_network}
\alias{simulate_temporal_network}
\title{Simulate a temporal network as edge-activity spells}
\usage{
simulate_temporal_network(
  nodes,
  periods,
  initial_probability = 0.1,
  formation_probability = 0.05,
  dissolution_probability = 0.1,
  directed = TRUE,
  loops = FALSE,
  weight = c("binary", "uniform", "normal", "poisson"),
  weight_mean = 1,
  weight_sd = 1,
  weight_range = c(0.1, 1),
  node_type = NULL,
  seed = NULL
)
}
\arguments{
\item{nodes}{Number of nodes or unique node labels.}

\item{periods}{Number of discrete observation periods.}

\item{initial_probability}{Probability that a dyad is active initially.}

\item{formation_probability}{Probability that an inactive dyad forms at the
next period.}

\item{dissolution_probability}{Probability that an active dyad dissolves at
the next period.}

\item{directed}{Generate directed dyads.}

\item{loops}{Permit self-loops.}

\item{weight}{Edge-spell weight distribution. One weight is drawn per spell
and held constant while the spell is active.}

\item{weight_mean, weight_sd, weight_range}{Weight parameters.}

\item{node_type}{Optional node type for each node.}

\item{seed}{Optional seed.}
}
\value{
A tidy spell-level \code{simulab_sim} base \code{data.frame} with one row per
activity spell and columns \code{from}, \code{to}, \code{onset}, \code{terminus}, \code{weight},
and \code{censored}. \code{terminus} is exclusive: the spell covers periods \code{onset}
through \code{terminus - 1}, so a spell still active in the final period has
\code{terminus == periods + 1} and \code{censored == TRUE}.
\code{as.data.frame(x, what = )} also returns \code{events} (\code{from}, \code{to}, \code{time},
\code{event} -- \code{"formation"} or \code{"dissolution"} -- and \code{weight}, ordered by
time), \code{snapshots} (one row per active dyad per period: \code{period}, \code{from},
\code{to}, \code{weight}), \code{nodes} (\code{node}, \code{type}), and a one-row \code{settings} table.
}
\description{
Simulate a temporal network as edge-activity spells
}
\examples{
result <- simulate_temporal_network(
  nodes = 20, periods = 4, initial_probability = 0.1, seed = 1
)
head(result)
components(result)
}
