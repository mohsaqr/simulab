% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tna.R
\name{simulate_group_tna}
\alias{simulate_group_tna}
\title{Simulate sequences and fit a grouped TNA model}
\usage{
simulate_group_tna(
  groups,
  actors,
  transitions = NULL,
  chain_length,
  initial = NULL,
  group_names = NULL,
  states = NULL,
  n_states = 5L,
  state_categories = NULL,
  model = c("tna", "ftna", "ctna", "atna"),
  seed = NULL,
  ...
)
}
\arguments{
\item{groups}{Number of groups.}

\item{actors}{Actors per group, scalar or one value per group.}

\item{transitions}{A common transition matrix, one matrix per group, or
\code{NULL} for randomly generated matrices. A tidy data frame with columns
\code{group}, \code{from}, \code{to} and \code{probability} gives one matrix per group.}

\item{chain_length}{Sequence length.}

\item{initial}{Common initial probabilities, one vector per group, or
\code{NULL}. A tidy data frame with columns \code{group}, \code{state} and \code{probability}
gives one vector per group.}

\item{group_names}{Optional group labels.}

\item{states, n_states, state_categories}{State-space arguments passed to
\code{simulate_sequences()}.}

\item{model}{TNA model type.}

\item{seed}{Optional random seed. Group-specific deterministic offsets are
used without leaking RNG state.}

\item{...}{Advanced sequence arguments.}
}
\value{
A long-form \code{simulab_sim} base \code{data.frame} of grouped sequences with
columns \code{group}, \code{id}, \code{period}, and \code{state}.
\code{as.data.frame(x, what = )} also returns \code{true_transitions} (the
generating probabilities), \code{estimated_edges} (the fitted TNA weights),
\code{model_info}, \code{wide}, and \code{groups}. The native \code{group_tna} model is
accessible through \code{as_tna_model()}. Requires the suggested \code{tna} package.
}
\description{
Simulate sequences and fit a grouped TNA model
}
\examples{
if (requireNamespace("tna", quietly = TRUE)) {
  result <- simulate_group_tna(
    groups = 2, actors = 20, chain_length = 12, n_states = 3, seed = 1
  )
  head(result)
  components(result)
}
}
