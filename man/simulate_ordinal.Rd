% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{simulate_ordinal}
\alias{simulate_ordinal}
\title{Simulate correlated ordinal variables}
\usage{
simulate_ordinal(
  n,
  probabilities,
  n_variables = 1L,
  rho = 0,
  structure = c("independent", "exchangeable", "ar1", "custom"),
  correlation = NULL,
  labels = NULL,
  variable_names = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations.}

\item{probabilities}{A numeric vector used for every variable or a matrix
with one row per variable and one column per category.}

\item{n_variables}{Number of variables when \code{probabilities} is a vector.}

\item{rho, structure, correlation}{Correlation arguments for the latent
normal variables. \code{structure} follows the same resolution rule as
\code{\link[=simulate_correlated]{simulate_correlated()}}: leaving it unset selects \code{"exchangeable"} when a
non-zero \code{rho} is given. Passed to the latent
Gaussian generator.}

\item{labels}{Optional category labels.}

\item{variable_names}{Optional variable names.}

\item{seed}{Optional random seed.}
}
\value{
A \code{simulab_sim} base \code{data.frame} with one row per observation.
}
\description{
Simulate correlated ordinal variables
}
\examples{
result <- simulate_ordinal(
  n = 200, probabilities = c(0.2, 0.5, 0.3), n_variables = 2, rho = 0.4, seed = 1
)
head(result)
as.data.frame(result, what = "probabilities")
}
