% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/networks.R
\name{simulate_sequences}
\alias{simulate_sequences}
\title{Simulate basic or perturbed state sequences}
\usage{
simulate_sequences(
  n,
  transition = NULL,
  chain_length,
  initial = NULL,
  states = NULL,
  n_states = 5L,
  state_categories = NULL,
  concentration = 1,
  missing_tail = c(0L, 0L),
  stable_transitions = NULL,
  stability_probability = 0.95,
  instability = c("none", "random_jump", "perturb", "unlikely_jump"),
  instability_probability = 0.4,
  perturbation = 0.5,
  unlikely_threshold = 0.1,
  seed = NULL
)
}
\arguments{
\item{n}{Number of sequences.}

\item{transition}{Optional transition matrix. When \code{NULL}, a random matrix
is generated.}

\item{chain_length}{Maximum sequence length.}

\item{initial}{Initial probabilities or a fixed starting state.}

\item{states}{State labels.}

\item{n_states}{Number of automatically generated states.}

\item{state_categories}{Optional learning-state categories used to name an
automatically generated state space.}

\item{concentration}{Positive Dirichlet concentration used for automatic
transition and initial probabilities.}

\item{missing_tail}{Number or range of trailing positions removed from each
sequence.}

\item{stable_transitions}{Optional two-column data frame named \code{from} and
\code{to} defining preferred transitions.}

\item{stability_probability}{Probability of following a preferred transition.}

\item{instability}{Instability mechanism for other transitions.}

\item{instability_probability}{Probability of applying that mechanism.}

\item{perturbation}{Multiplicative probability perturbation magnitude.}

\item{unlikely_threshold}{Maximum probability considered unlikely.}

\item{seed}{Optional random seed.}
}
\value{
A long-form \code{simulab_sim} base \code{data.frame}. Wide sequences,
transition probabilities, initial probabilities, and settings are tidy
components.
}
\description{
Simulate basic or perturbed state sequences
}
\examples{
result <- simulate_sequences(n = 40, n_states = 4, chain_length = 20, seed = 1)
head(result)
components(result)

# Preferred transitions followed with a given probability.
stable <- data.frame(
  from = sprintf("State \%d", 1:4),
  to = sprintf("State \%d", c(2, 3, 4, 1))
)
head(simulate_sequences(
  n = 40, n_states = 4, chain_length = 20,
  stable_transitions = stable, stability_probability = 0.85, seed = 1
))
}
